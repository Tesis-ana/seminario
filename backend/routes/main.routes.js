const express = require('express');
const router = express.Router();

// // Importar routers específicos
// router.use('/users', require('./user.routes'));
// router.use('/pacientes', require('./paciente.routes'));
// router.use('/imagenes', require('./imagen.routes'));
// router.use('/segmentaciones', require('./segmentacion.routes'));
// router.use('/pwatscore', require('./pwatscore.routes'));

module.exports = router;
